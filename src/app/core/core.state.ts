import {ActionReducerMap} from '@ngrx/store';


// tslint:disable-next-line:no-empty-interface
export interface AppState {
  // no global state yet. Use the line below as a template to add!
  // myState: myReducer.MyState;
}

export const reducers: ActionReducerMap<AppState> = {
  // no global reducers yet. Use the line below as a template to add!
  // myState: myReducer.reducer
};
